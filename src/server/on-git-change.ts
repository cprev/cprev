'use strict';

import {ChangePayload, GitPayload, ResultCallback} from "../types";

export const remotesToRepo = new Map<string, string>();

export const onGitChange = (p: GitPayload, cb: ResultCallback) => {




};
